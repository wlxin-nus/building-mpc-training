# Local example only. The CLI reads the process environment; it does not load this file.
# Use the credential-free BOPTEST origin reachable from the launching process.
# The exposed port depends on your BOPTEST deployment (commonly 8000, 5000, or 80).
BOPTEST_URL=http://127.0.0.1:8000
